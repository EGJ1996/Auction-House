@model Auction.Models.SuggestedItem

@{
    ViewBag.Title = "Details";
}

<h2>Suggested Item Details</h2>

<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Description)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Description)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Rating)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Rating)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.PhotoData)
        </dt>

        <dd>
            <img src="@Url.Action("GetImage", "SuggestedItem", new { Model.ID })" alt="Image" />
        </dd>

    </dl>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="button" value="Approve" class="btn btn-default" onclick="@("window.location.href='" + @Url.Action("Approve", "SuggestedItem", new { id = Model.ID }) + "'");" />
        </div>
    </div>

</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.ID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
